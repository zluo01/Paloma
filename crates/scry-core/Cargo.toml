[package]
name = "scry-core"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "UI-neutral application core: storage, controllers, background managers, and the shared app context"

[dependencies]
tokio = { workspace = true, features = ["rt", "fs", "sync"] }
thiserror = { workspace = true }
reqwest = { workspace = true }
scry-capability = { path = "../scry-capability" }
scry-config = { path = "../scry-config" }
scry-controller = { path = "../scry-controller" }
scry-permission = { path = "../scry-permission" }
scry-storage = { path = "../scry-storage" }
